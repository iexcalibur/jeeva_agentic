# Core Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.9.0,<3.0
pydantic-settings>=2.1.0

# LangChain & LangGraph
langchain>=0.1.0,<0.3
langchain-anthropic>=0.1.0
langgraph>=0.0.20
# langchain-core is installed as a dependency of langchain, no need to specify separately

# Database
# Note: asyncpg may not support Python 3.14 yet. Use Python 3.11 or 3.12 if you encounter build errors.
asyncpg>=0.29.0  # For PostgreSQL
aiosqlite>=0.19.0  # For SQLite

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Development
black==23.12.0
ruff==0.1.8

# Cache
redis>=5.0.0  # For Redis cache (optional, falls back to in-memory if not available)

# Utilities
requests==2.31.0
docker==7.0.0

